{
  "name": "high-performance-kline-engine",
  "version": "1.0.0",
  "description": "A high-performance K-line chart engine using Canvas 2D API.",
  "main": "src/main.ts",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "typescript": "^5.0.0",
    "vite": "^4.0.0"
  }
}
